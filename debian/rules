#!/usr/bin/make -f

%:
	dh $@ --with autoreconf --parallel

override_dh_autoreconf:
	./bootstrap

override_dh_auto_build:
	dh_auto_build -- all plugins

#override_dh_auto_install:
#	dh_auto_install -- plugins-install
